[project]
name = "obs-platform"
version = "0.1.0"
description = "Observatory platform modules: ingestion, models, simulation, analytics, tools."
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/obs_platform"]
